<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HashCheck.Views.MediaGroupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:HashCheck.ViewModels"
    xmlns:conv="using:HashCheck.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <conv:BoolToOnlineBrushConverter x:Key="BoolToOnlineBrushConverter"/>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title / subtitle -->
        <TextBlock Grid.Row="0"
                   Text="{x:Bind ViewModel.MediaName, Mode=OneWay}"
                   Style="{ThemeResource TitleTextBlockStyle}"
                   Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1"
                   Text="{x:Bind ViewModel.Description, Mode=OneWay}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Margin="0,0,0,16"/>

        <!-- Column headers -->
        <Grid Grid.Row="2" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"/>
            <TextBlock Grid.Column="1" Text="Label"         FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2" Text="Drive"         FontWeight="SemiBold"/>
            <TextBlock Grid.Column="3" Text="Scan Root"     FontWeight="SemiBold" Margin="8,0,0,0"/>
            <TextBlock Grid.Column="4" Text="Last Verified" FontWeight="SemiBold"/>
            <TextBlock Grid.Column="5" Text="Status"        FontWeight="SemiBold"/>
        </Grid>

        <!-- Volume rows -->
        <ListView
            Grid.Row="3"
            ItemsSource="{x:Bind ViewModel.Volumes}"
            SelectedItem="{x:Bind ViewModel.SelectedRow, Mode=TwoWay}"
            SelectionMode="Single">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="4,6"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:VolumeRow">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="●" FontSize="10" VerticalAlignment="Center"
                                   Foreground="{x:Bind IsOnline, Converter={StaticResource BoolToOnlineBrushConverter}}"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind Label}"        TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="2" Text="{x:Bind MountPoint}"/>
                        <TextBlock Grid.Column="3" Text="{x:Bind ScanScope}"    TextTrimming="CharacterEllipsis" Margin="8,0,0,0"/>
                        <TextBlock Grid.Column="4" Text="{x:Bind LastVerified,  Mode=OneWay}"/>
                        <TextBlock Grid.Column="5" Text="{x:Bind Status,        Mode=OneWay}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Action bar -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8" Margin="0,12,0,0">
            <Button Content="Validate"
                    Click="Validate_Click"
                    IsEnabled="{x:Bind ViewModel.CanValidateSelected, Mode=OneWay}"
                    Style="{ThemeResource AccentButtonStyle}"
                    ToolTipService.ToolTip="Validate the selected volume. To validate all volumes in this group, use the Dashboard."/>
            <Button Content="Edit Mirror Root"
                    Click="EditScanPath_Click"
                    IsEnabled="{x:Bind ViewModel.HasSelectedRow, Mode=OneWay}"/>
            <Button Content="Back" Click="Back_Click"/>
        </StackPanel>
    </Grid>
</Page>
